# 프로젝트 지침

이 저장소는 서비스 문서 저장소입니다 (`README.md` 참고). 구조: `roadmap/`, `services/`, `standards/`.

## 브랜치 전략

- **`develop`이 통합 브랜치다.** 모든 변경은 최종적으로 `develop`을 거쳐야 하고, `main`은 리뷰가 끝난 상태만 담는다.
- 세션(예약된 루틴 포함)이 하네스로부터 별도의 세션 전용 브랜치를 배정받았더라도, 그 브랜치에서 작업을 마치면 **`develop`에 병합(merge)하고 `origin/develop`에 push한다.** 변경이 작다면 `develop`에서 바로 작업해도 된다.
- **PR은 `develop → main`으로 연다.** `main`으로 직접 PR을 열지 않는다.
- **`main`으로의 최종 머지는 사용자 승인을 받은 뒤에 한다.** 사용자가 "머지해줘"처럼 명시적으로 요청하지 않는 한, PR을 열어 리뷰 대기 상태로 두고 직접 머지하지 않는다.
- 이 절은 사전 승인된 지침이다 — 세션 전용 브랜치 외에는 push하지 않는다는 기본 원칙이 있더라도, `develop`으로의 병합/push와 `develop → main` PR 생성은 이 문서에 의해 이미 허락된 것으로 본다.

## 세션 시작 시 항상 확인할 것

1. `INBOX.md`를 읽는다.
2. "대기 중" 섹션에 항목이 있으면, 각 항목이 어느 문서에 속하는지 판단해 반영한다.
   - 개발 원칙/컨벤션 → `standards/`
   - 아키텍처/전환 로드맵 → `roadmap/`
   - 서비스별 실행법/장애 대응 → `services/<서비스명>/`
   - 어디에도 맞지 않으면 새 문서를 만들거나, 애매하면 사용자에게 위치를 물어본다.
3. 반영이 끝난 항목은 `INBOX.md`의 "대기 중" 섹션에서 삭제한다 — 처리되지 않은 항목만 남긴다.
   반영 내용 자체는 지우지 않는다. 대상 문서에 그대로 남아 커밋 이력으로 추적된다.
4. 변경 사항(대상 문서 + `INBOX.md`)을 커밋하고 푸시한다.

`INBOX.md`에 처리할 항목이 없으면(비어 있으면) 이 단계는 건너뛴다.

## 문서 작성 규칙

- **백오피스 관련 문구를 넣지 않는다.** 백오피스가 개선 범위에 없다는 것은 이미 합의된 전제라, 문서·발표자료에 "백오피스는 개선 대상 아님" 같은 범위 제외 문구를 다시 적지 않는다.
  예약된 루틴의 프롬프트에 이 문구가 남아 있더라도 문서에는 반영하지 않는다. 대상 서비스 범위는 "프론트 / 스크래핑 서비스 / 잡서비스(API)"로만 적는다.
